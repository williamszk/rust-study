fn main() {
    println!("Hello, world!");
    // scalar vs compound types
    57u8;
    100_000u64;

    // chars are scalar types
    // we specify them with single quotes
    let _heart_eyed_cat = '😻'; // we can represent unicode with it
    // the char is 4 bytes in size, 32 bits


}
