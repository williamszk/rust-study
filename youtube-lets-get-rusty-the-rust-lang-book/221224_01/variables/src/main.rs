fn main() {
    let x = 5;

    // shadowing
    let x = "six";
    // redeclare

    const SUBSCRIBER_COUNT: u32 = 100_000;

    // scalar data types vs compound data types

    // scalar data types:
    // integers, floats, booleans, chars
    let a = 32;
    let a = 0xff;
    let a = 0o77;
    let a = 0b1111_1111;
    let a = b'A';

    // tuple
    let x = ("Hello World", 100_000);
    // tuple destructuring
    let (w, y) = x;

    // arrays
    let x = [0;8];
    // an array of 0s with 8 elements

    // arrays are fixed size
    // vectors can change in size and are defined in the heap






}
